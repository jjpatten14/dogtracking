{
  "name": "mcp-memory-server",
  "version": "0.6.3",
  "description": "Standalone MCP memory server for Claude Code",
  "type": "module",
  "bin": {
    "mcp-memory-server": "dist/index.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/*.js",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.2"
  }
}